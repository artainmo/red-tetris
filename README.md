# red-tetris

In the front-end we must not use classes but we can use any framework. As in the given boilerplate we will use React for the frontend.

In the backend we will simply use javascript and test it in the terminal with nodejs.