# red-tetris

In the front-end we must not use classes but we can use any framework. As in the given boilerplate we will use React for the frontend. It must be a single-page-application. We will also use Redux.

In the backend we will simply use javascript with nodejs. We will use PostgreSQL as database.
